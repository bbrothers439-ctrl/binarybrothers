import { Layout } from "@/components/layout/Layout";
import { Link } from "wouter";
import { motion } from "framer-motion";
import { Cpu, Zap, Shield, ChevronRight } from "lucide-react";
import logoImg from "@assets/Gemini_Generated_Image_8jk2zi8jk2zi8jk2_1782657103447.png";

export default function Home() {
  return (
    <Layout>
      {/* Hero */}
      <section className="relative pt-32 pb-20 md:pt-48 md:pb-32 overflow-hidden">
        <div className="container mx-auto px-4 text-center z-10 relative">
          <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6 }}
            className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/5 border border-white/10 text-primary mb-8">
            <span className="relative flex h-2 w-2">
              <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75" />
              <span className="relative inline-flex rounded-full h-2 w-2 bg-primary" />
            </span>
            <span className="text-xs font-mono tracking-widest uppercase">System Online</span>
          </motion.div>

          <motion.div initial={{ opacity: 0, scale: 0.92 }} animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.7, delay: 0.1 }} className="flex justify-center mb-6">
            <img src={logoImg} alt="Binary Brothers Logo"
              className="w-72 md:w-96 lg:w-[480px] object-contain drop-shadow-[0_0_40px_rgba(0,240,255,0.35)]" />
          </motion.div>

          <motion.p initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.2 }}
            className="text-xl md:text-2xl text-white/60 max-w-2xl mx-auto mb-10">
            AI-powered websites built for tomorrow. We engineer digital experiences with unmatched precision and speed.
          </motion.p>

          <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.3 }}
            className="flex flex-col sm:flex-row items-center justify-center gap-4">
            <Link href="/services"
              className="w-full sm:w-auto px-8 py-4 bg-primary text-primary-foreground font-semibold rounded-lg hover:bg-primary/90 transition-all shadow-[0_0_20px_rgba(0,240,255,0.4)] hover:shadow-[0_0_30px_rgba(0,240,255,0.6)] flex items-center justify-center gap-2">
              View Services <ChevronRight className="w-4 h-4" />
            </Link>
            <Link href="/contact"
              className="w-full sm:w-auto px-8 py-4 bg-white/5 border border-white/10 text-white font-semibold rounded-lg hover:bg-white/10 transition-all flex items-center justify-center gap-2">
              Contact Mission Control
            </Link>
          </motion.div>
        </div>
      </section>

      {/* Features Grid */}
      <section className="py-20 md:py-32 relative border-t border-white/5 bg-black/20">
        <div className="container mx-auto px-4">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-5xl font-display font-bold text-white mb-4">Engineering the Future</h2>
            <p className="text-white/50 max-w-xl mx-auto">Our AI-augmented approach transforms how digital products are built, managed, and scaled.</p>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {[
              { icon: Cpu, title: "AI-Powered Architecture", desc: "We leverage advanced AI models to architect and generate flawless, highly-optimized code structures instantly." },
              { icon: Zap, title: "Hyper-Speed Delivery", desc: "What takes traditional agencies months, we accomplish in days. Speed without sacrificing a single pixel of quality." },
              { icon: Shield, title: "Continuous Management", desc: "We don't just build. Our intelligent systems monitor, update, and manage your platform 24/7." },
            ].map((feature, i) => (
              <motion.div key={i} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }} transition={{ duration: 0.5, delay: i * 0.1 }}
                className="glass-panel p-8 rounded-2xl relative group hover:border-primary/50 transition-colors">
                <div className="w-12 h-12 rounded-lg bg-primary/10 border border-primary/20 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform">
                  <feature.icon className="w-6 h-6 text-primary" />
                </div>
                <h3 className="text-xl font-display font-semibold text-white mb-3">{feature.title}</h3>
                <p className="text-white/60 leading-relaxed">{feature.desc}</p>
                <div className="absolute inset-0 bg-gradient-to-br from-primary/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity rounded-2xl pointer-events-none" />
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Terminal Visual */}
      <section className="py-20 md:py-32 relative">
        <div className="container mx-auto px-4">
          <div className="glass-panel rounded-3xl overflow-hidden border border-white/10 shadow-[0_0_50px_rgba(0,0,0,0.5)] max-w-5xl mx-auto">
            <div className="bg-white/5 border-b border-white/10 px-4 py-3 flex items-center gap-2">
              <div className="w-3 h-3 rounded-full bg-red-500" />
              <div className="w-3 h-3 rounded-full bg-yellow-500" />
              <div className="w-3 h-3 rounded-full bg-green-500" />
              <div className="ml-4 flex-1 text-center text-xs text-white/40 font-mono">binary-brothers-core // build-process</div>
            </div>
            <div className="p-6 md:p-10 font-mono text-sm md:text-base text-white/70 overflow-x-auto">
              <div className="flex gap-4 opacity-50 mb-2"><span className="text-primary">❯</span><span>Initializing project protocol...</span></div>
              <div className="flex gap-4 mb-2"><span className="text-primary">❯</span><span>Connecting to AI generation nodes... <span className="text-green-400">SUCCESS</span></span></div>
              <div className="flex gap-4 mb-2"><span className="text-primary">❯</span><span>Compiling frontend architecture...</span></div>
              <motion.div initial={{ opacity: 0 }} whileInView={{ opacity: 1 }} transition={{ duration: 1, delay: 0.5 }}
                className="pl-8 text-primary/80 my-4 border-l-2 border-primary/30 py-2">
                100% ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓<br/>
                Build complete in 0.42s.<br/>
                Deploying to edge network...
              </motion.div>
              <motion.div initial={{ opacity: 0 }} whileInView={{ opacity: 1 }} transition={{ duration: 0.1, delay: 1.5 }} className="flex gap-4">
                <span className="text-primary">❯</span>
                <span className="text-white glow-text">Mission critical system online. Ready for command.</span>
                <span className="w-2 h-5 bg-primary animate-pulse inline-block ml-1 align-middle" />
              </motion.div>
            </div>
          </div>
        </div>
      </section>
    </Layout>
  );
}
